# PaddleOCR + PP-Structure stack for offline wheel install
# (Windows amd64, Python 3.12 — matches project .venv).
# Install after core packages:
#   pip install --no-index --find-links=wheels --find-links=wheels/paddleocr -r requirements-paddleocr-wheels.txt

paddlepaddle==3.3.1
paddleocr==3.7.0
paddlex[ocr]==3.7.2
